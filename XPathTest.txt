for $a in doc("small.xml")//ACT,
    $b in doc("small.xml")//ACT,
    $c in doc("small.xml")//ACT,
        $sa in $a//SCENE,
        $sb in $b//SCENE,
        $sc in $c//SCENE
where $sa eq $sb and $sa eq $sc and $sb eq $sc
return <result>{ <title>{$sa/TITLE/text()}</title> }</result>